document.querySelectorAll("[data-time],.clock").forEach(e=>setInterval(()=>e.textContent=new Date().toLocaleTimeString(),1000));(function(){const c=document.getElementById("gridRain");if(!c)return;const x=c.getContext("2d");let w,h,cols,drops,size=16;const g=["01","7","Δ","SIG","HUM","ASSET","CUTOUT","BLACK","ARRAY","GRID","CELL","██","KILO","GHOST"];function r(){w=c.width=innerWidth;h=c.height=innerHeight;cols=Math.floor(w/size);drops=Array(cols).fill(0).map(()=>Math.random()*h/size)}r();addEventListener("resize",r);const p=a=>a[Math.floor(Math.random()*a.length)];function d(){x.fillStyle="rgba(0,0,0,.08)";x.fillRect(0,0,w,h);x.font=size+"px Consolas";for(let i=0;i<drops.length;i++){const rare=Math.random()<.006;x.fillStyle=rare?"rgba(255,176,0,.95)":"rgba(0,229,255,.28)";x.fillText(rare?p(["THE TARGET IS NOT THE TARGET","FOLLOW THE MONEY","HANDLER LIES ONCE"]):p(g),i*size,drops[i]*size);if(drops[i]*size>h&&Math.random()>.975)drops[i]=0;drops[i]+=.65}requestAnimationFrame(d)}d()})();